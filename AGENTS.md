# actions-runner fork notes

- This fork tracks upstream runner versions by adding 10 to the upstream major version. For example, upstream `2.335.0` should be released here as `12.335.0`.
- Keep `releaseVersion` and `src/runnerversion` in sync.
